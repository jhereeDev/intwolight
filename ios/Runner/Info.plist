<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>In Two Lights</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>In Two Lights</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- Portrait only. The corner scene fits by width and the HUD and level
	     map were laid out for a tall aspect; landscape was never designed and
	     App Review does open the iPad layout. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
	</array>
	<!-- ⚠️ MANDATORY whenever TARGETED_DEVICE_FAMILY includes iPad ("1,2") and the
	     iPad orientations are not all four. Without it altool rejects the upload
	     outright with error 90474 — not at review, at upload — demanding all four
	     orientations "to support iPad multitasking". It did exactly that to the
	     first IPA that ever built and signed, 2026-08-04.

	     This key is the escape hatch: it declares the app does not support Slide
	     Over or Split View, so the four-orientation rule no longer applies. The
	     alternative is real landscape support on iPad, which is a design change,
	     not a plist change — landscape was never laid out.

	     sarimanok carries the identical config and shipped with it. The port to
	     this repo dropped this one key, and only this one. -->
	<key>UIRequiresFullScreen</key>
	<true/>
	<!-- Export compliance. Without this key TestFlight parks EVERY build behind a
	     "Missing Compliance" prompt that has to be answered by hand in App Store
	     Connect before the build can be installed — it did exactly that to build 6,
	     2026-08-04, and did it to sarimanok's build 2 before that.

	     False is the honest answer: the only encryption here is standard HTTPS to
	     RevenueCat, made through Apple's own networking stack, which is exempt. The
	     app bundles no crypto library and implements no algorithm of its own. This
	     is the same declaration answered by hand for build 6 ("None of the
	     algorithms mentioned above"), and Apple's own dialog recommends moving it
	     into the plist. Revisit if the app ever ships its own crypto. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
